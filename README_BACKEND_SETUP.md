# 🐍 Настройка Backend - Краткая инструкция

## 📝 Что нужно сделать:

### 1. Создать .env файл в директории `backend/`

```bash
cd backend
nano .env
```

Добавьте (замените значения):

```env
DATABASE_URL=postgresql+psycopg2://softstudio_user:ВАШ_ПАРОЛЬ@localhost:5432/softstudio_db
SECRET_KEY=ВАШ_СЕКРЕТНЫЙ_КЛЮЧ
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=14
```

**Где взять значения:**
- `ВАШ_ПАРОЛЬ` - пароль, который вы установили для пользователя `softstudio_user` в PostgreSQL
- `ВАШ_СЕКРЕТНЫЙ_КЛЮЧ` - сгенерируйте командой:
  ```bash
  python3 -c "import secrets; print(secrets.token_urlsafe(32))"
  ```

### 2. Создать таблицы в базе данных

```bash
# Активируйте виртуальное окружение (если еще не активировано)
source venv/bin/activate

# Выполните миграции
python -m app.migration
```

Вы должны увидеть:
```
✓ Все таблицы созданы/обновлены
```

### 3. Запустить сервер

```bash
uvicorn app.main:app --reload
```

Сервер запустится на http://localhost:8000

## ✅ Проверка

1. Откройте http://localhost:8000/docs - должна открыться документация API
2. Откройте http://localhost:8000/tables - должны отобразиться созданные таблицы

## 🔧 Автоматическая настройка

Используйте скрипт:

```bash
cd backend
chmod +x setup_backend.sh
./setup_backend.sh
```

Скрипт автоматически выполнит все шаги выше.

---

**Подробная инструкция:** [CREATE_TABLES.md](./CREATE_TABLES.md)
