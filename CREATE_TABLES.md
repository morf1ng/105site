# 🗄️ Создание таблиц в базе данных

Инструкция по созданию всех необходимых таблиц в PostgreSQL.

## 📋 Предварительные требования

1. ✅ База данных `softstudio_db` создана
2. ✅ Пользователь `softstudio_user` создан и имеет права
3. ✅ Backend зависимости установлены

## 🚀 Способ 1: Автоматический (рекомендуется)

Используйте скрипт настройки:

```bash
cd backend
chmod +x setup_backend.sh
./setup_backend.sh
```

Скрипт автоматически:
- Создаст виртуальное окружение
- Установит зависимости
- Настроит `.env` файл
- Выполнит миграции (создаст таблицы)

## 🛠️ Способ 2: Ручной

### Шаг 1: Настройка .env файла

Создайте файл `backend/.env`:

```bash
cd backend
nano .env
```

Добавьте следующее содержимое (замените значения на свои):

```env
DATABASE_URL=postgresql+psycopg2://softstudio_user:ваш_пароль@localhost:5432/softstudio_db
SECRET_KEY=ваш_секретный_ключ_минимум_32_символа
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=14
```

**Генерация SECRET_KEY:**
```bash
python3 -c "import secrets; print(secrets.token_urlsafe(32))"
```

### Шаг 2: Активация виртуального окружения

```bash
# Если виртуальное окружение еще не создано
python3 -m venv venv

# Активируйте его
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows
```

### Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 4: Выполнение миграций

```bash
python -m app.migration
```

Вы должны увидеть:
```
✓ Все таблицы созданы/обновлены
```

## ✅ Проверка

### Проверка через PostgreSQL:

```bash
# Подключитесь к базе данных
psql -U softstudio_user -d softstudio_db -h localhost

# В консоли PostgreSQL выполните:
\dt

# Вы должны увидеть список таблиц:
# - project
# - project_about_company
# - project_stage
# - project_result
# - project_result_image
# - project_progress
# - roles
# - users
```

### Проверка через API:

После запуска backend сервера:

```bash
curl http://localhost:8000/tables
```

Или откройте в браузере: http://localhost:8000/tables

## 📊 Созданные таблицы

После выполнения миграций будут созданы следующие таблицы:

1. **project** - Основная информация о проектах
2. **project_about_company** - Описание компании (1:1 с project)
3. **project_stage** - Этапы проекта (1:N с project)
4. **project_result** - Результаты проекта (1:1 с project)
5. **project_result_image** - Изображения результатов (1:N с project_result)
6. **project_progress** - Прогресс проекта (1:N с project)
7. **roles** - Роли пользователей
8. **users** - Пользователи системы

## 🔧 Troubleshooting

### Ошибка: "could not connect to server"

**Решение:**
1. Проверьте, что PostgreSQL запущен:
   ```bash
   sudo systemctl status postgresql
   ```

2. Проверьте правильность DATABASE_URL в `.env`

3. Проверьте, что пользователь и база данных существуют:
   ```bash
   sudo -u postgres psql -c "\du" | grep softstudio_user
   sudo -u postgres psql -c "\l" | grep softstudio_db
   ```

### Ошибка: "password authentication failed"

**Решение:**
1. Проверьте правильность пароля в `.env`
2. Убедитесь, что пользователь создан:
   ```bash
   sudo -u postgres psql -c "ALTER USER softstudio_user WITH PASSWORD 'новый_пароль';"
   ```

### Ошибка: "ModuleNotFoundError: No module named 'app'"

**Решение:**
1. Убедитесь, что вы в директории `backend`
2. Активируйте виртуальное окружение
3. Установите зависимости: `pip install -r requirements.txt`

---

**После успешного создания таблиц можно запускать backend сервер!**
